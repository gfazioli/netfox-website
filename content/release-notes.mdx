---
title: Release Notes
---

import { ReleaseNotes } from "@/components/ReleaseNotes/ReleaseNotes";
import { loadReleases } from "@/components/ReleaseNotes/load-releases";

{/*
  Fetched and compiled at build time, so the releases are in the served HTML:
  fetched in the browser, Googlebot got a 403 from the API route and saw only the
  loading skeleton.
*/}
export const releases = await loadReleases();

# Release Notes

Auto-fetched from the [Netfox releases on GitHub](https://github.com/gfazioli/netfox-website/releases). Each release lists what's new, what's fixed, and any upgrade notes that matter.

The same history is inside the app: **Help → Release Notes…** opens the whole history, newest first, offline and in your language.

<ReleaseNotes initialReleases={releases} />
